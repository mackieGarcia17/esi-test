import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './Components/App.jsx'

const root = ReactDOM.createRoot(document.getElementById('esi-test'))
root.render(
    <React.StrictMode>
        <App />
    </React.StrictMode>,
)
